<div dir="rtl" align="center">

# 🗺️ خارطة طريق QRFerry — خطة تطوير الأداة

**الرؤية:** تحويل QRFerry من «تجربة تقنية مذهلة» إلى «أداة يومية قوية» يعتمد
عليها الناس في البيئات العادية والمعزولة أمنياً.

> **حالة التنفيذ:** ✅ اكتمل تنفيذ المراحل الثلاث (الإصدار 1.1) — راجع `git log` واختبارات E2E في `e2e/`.

</div>

---

## المبادئ الحاكمة (قبل أي ميزة)

1. **لا شيء يغادر الجهاز** — كل ميزة تحترم مبدأ «صفر خوادم».
2. **التحقق قبل التباهي** — كل ميزة تُختبر E2E (كاميرا محاكاة) قبل إطلاقها.
3. **لا تكسير** — تعديلات جراحية، والبروتوكول البصري الحالي (QF4) يبقى ثابتاً.
4. **الجوال أولاً** — معظم المستخدمين على الهاتف.

---

# 🥇 المرحلة 1 — أداة يومية (أعلى قيمة)

> الهدف: حل 80% من «المشاكل اليومية» للمستخدم العادي.

## 1.1 نقل عدة ملفات / مجلد كامل دفعة واحدة

| | |
|---|---|
| **ماذا** | اختيار أكثر من ملف (أو مجلد) → تُجمَّع في حزمة واحدة → تُنقل ببث واحد → تصل كملفات منفصلة بأسمائها ومساراتها. |
| **لماذا** | أكبر قيد حالي: ملف واحد فقط. الواقع أن المستخدم يريد نقل «مشروع» أو «مجلد صور» كاملاً. |
| **تقنياً** | ملفات متعددة + شجرة مسارات → تُعبأ في حاوية (تحويل الحاوية البصرية لصيغة أرشيف بسيطة داخلية مع جدول مسارات) → يُخزَّن الجدول في رأس الحاوية. لا يتغير تنسيق الإطارات (QF4) — فقط محتوى الحاوية يثري. الاستقبال يفك الجدول ويحمّل كل ملف. |
| **الجهد** | متوسط (M) — يؤثر على `optical-transfer.ts` + `send-client` + `scanner-client` + الاختبارات. |
| **مخاطر** | حجم الجدول الثنائي (أسماء طويلة)؛ يُحل بترميز مكثّف + حد معقول للأسماء. |

## 1.2 المشاركة من أندرويد (Web Share Target API)

| | |
|---|---|
| **ماذا** | تختار أي ملف من أي تطبيق (معرض الصور، الملفات، واتساب…) → «مشاركة عبر QRFerry» → يفتح الموقع جاهزاً للبث مباشرة. |
| **لماذا** | هذه الميزة هي التي تحوّل الموقع من «شئ تفتحه» إلى «أداة نظام» — يدخل في قائمة المشاركة كأي تطبيق. |
| **تقنياً** | إضافة `share_target` في الـ manifest (يتطلب PWA مثبّتاً) + مسار استقبال يستقبل الملف الممرَّر ويجهزه تلقائياً (المسار `/share` أو معالجة في الصفحة الرئيسية). |
| **الجهد** | صغير-متوسط (S-M) — manifest + صفحة صغيرة + ربط. |
| **مخاطر** | يعمل على أندرويد مع Chromium فقط (iOS لا يدعم share_target بعد)؛ يتطلب تثبيت PWA أولاً. |

## 1.3 استقبال من كاميرا اللابتوب (Desktop Webcam Receiver)

| | |
|---|---|
| **ماذا** | صفحة `/scan` تعمل على الحاسوب أيضاً: موبايل يُرسل → اللابتوب يستقبل بكاميرته (webcam). يصبح النقل **اتجاهين كاملين**. |
| **لماذا** | حالياً اللابتوب يرسل فقط. التبادل الحقيقي يحتاج الاتجاهين: (موبايل → كمبيوتر) و(كمبيوتر → موبايل). |
| **تقنياً** | نفس ماسح ZXing الموجود — فقط واجهة تسمح باختيار الكاميرا الخلفية/الأمامية + تحسين معايرة القصّ لشاشة صغيرة قريبة. |
| **الجهد** | صغير (S) — كود المسح جاهز؛ المطلوب عناصر واجهة + إعدادات كاميرا. |
| **مخاطر** | دقة كاميرا اللابتوب أقل من الموبايل → ننصح بوضع Robust؛ يحتاج HTTPS (متوفر على Vercel). |

## 1.4 حزمة نشر أوفلاين (Offline Deployment Package)

| | |
|---|---|
| **ماذا** | زر «تحميل النسخة الأوفلاين» → ملف واحد (zip) يحتوي الموقع كاملاً → يُنقل بـ USB إلى الشبكة المعزولة → يُشغَّل محلياً على أي جهاز بدون إنترنت. |
| **لماذا** | هذه هي **الميزة الجوهرية للنيش الحقيقي** (بيئات معزولة أمنياً). تجعل الأداة قابلة للنشر داخل شبكة مقفولة بسهولة. |
| **تقنياً** | ميزة بناء تولّد حزمة static كاملة (كل الصفحات + الأصول + WASM + الخطوط) قابلة للتشغيل بأي خادم بسيط أو حتى فتحها محلياً (مع ملاحظة: الكاميرا تحتاج HTTPS أو localhost — نولّد معها شهادة محلية/تعليمات تشغيل). |
| **الجهد** | متوسط-كبير (M-L) — تحتاج مسار بناء ثالث + سكربت تعبئة + توثيق تشغيل. |
| **مخاطر** | HTTPS للكاميرا على شبكة مغلقة (نحل بـ localhost أو شهادة ذاتية)؛ حجم الحزمة (~بضعة ميجابايت) مقبول. |

## 1.5 بث لعدة مستقبلين (Broadcast Mode)

| | |
|---|---|
| **ماذا** | شاشة واحدة تُرسل، وعدة تليفونات تمسح وتستقبل في نفس الوقت — كل واحد يحصل على الملف. |
| **لماذا** | RaptorQ «إذاعة» بطبيعته (الرموز تُبث للجميع). عرض فريد للفصول والاجتماعات: «امسح الكود واستلم الملف». |
| **تقنياً** | لا جديد في البروتوكول — فقط واجهة توضح الوضع + عدّاد مستقبلين؟ (لا يمكن معرفة عدد المستقبلين بصرياً؛ نكتفي بتسمية الوضع وتوجيه المستخدمين). |
| **الجهد** | صغير (S) — واجهة + نصوص إرشادية. |
| **مخاطر** | لا شيء تقني؛ فقط توقعات المستخدم (الكل يمسح نفس الشاشة). |

---

# 🥈 المرحلة 2 — تعميق الأمان (ما يميّز الأداة عن أي سكانر)

> الهدف: يصبح QRFerry الأداة المفضلة للبيئات الحساسة.

## 2.1 الترابط بكود QR بدل كتابة كلمة المرور

| | |
|---|---|
| **ماذا** | المرسل يعرض كلمة المرور على شكل QR؛ المستقبل يمسحه فيُتفق على المفتاح فوراً — بلا كتابة ولا أخطاء. |
| **لماذا** | كتابة كلمة مرور طويلة على الموبايل مزعجة وقابلة للخطأ؛ QR يجعل «الاتفاق السري» فورياً وصامتاً. |
| **تقنياً** | عند تفعيل التشفير، يعرض المرسل QR خاص بكلمة المرور (مشفّرة في QR)؛ المستقبل يمسحه فيملأ حقل كلمة المرور تلقائياً. تظل كلمة المرور نفسها تعمل يدوياً أيضاً (توافق). |
| **الجهد** | متوسط (M) — مولّد/قارئ QR للنص (ZXing يقرأ نصاً؛ نستخدم `fast_qr` أو مولّد بسيط للعرض). |
| **مخاطر** | من يصور الشاشة يرى كلمة المرور QR → تُعرض **مرة واحدة فقط** ثم تختفي (اختيار المستخدم). |

## 2.2 التوقيع الرقمي للملف (Sender Authenticity)

| | |
|---|---|
| **ماذا** | المرسل يوقّع الملف بمفتاحه الخاص؛ المستقبل يتحقق أن الملف **فعلاً من المرسل** (وليس فقط سليماً). |
| **لماذا** | في البيئات الحساسة: «هل هذا الملف أصلي من الجهة المعنية؟» — التحقق من الهوية لا فقط من السلامة. |
| **تقنياً** | WebCrypto ECDSA/P-256: مفتاح خاص عند المرسل (يُدار محلياً)، مفتاح عام يُتبادل بصرياً عبر QR (ترابط أول مرة). التوقيع يُضمَّن في رأس الحاوية؛ المستقبل يتحقق قبل الفك. |
| **الجهد** | كبير (L) — إدارة مفاتيح + واجهة ترابط + اختبارات. |
| **مخاطر** | إدارة المفاتيح معقدة للمستخدم العادي → نجعله وضعاً «متقدماً» اختيارياً. |

## 2.3 وضع «يُحذف بعد القراءة» (Burn After Reading)

| | |
|---|---|
| **ماذا** | المستقبل يعرض الملف مرة واحدة؛ بعد فتحه/حفظه يُمسح من التخزين المحلي (IndexedDB) تلقائياً. |
| **لماذا** | مواد حساسة جداً: من يمسح الشاشة لاحقاً لا يجد الملف على الجهاز. |
| **تقنياً** | خيار عند الإرسال يمرَّر في الميتاداتا؛ عند الاستقبال يُحذف بلوب الملف وبقايا الجلسة فور العرض. |
| **الجهد** | صغير-متوسط (S-M) — ميتاداتا + حذف مُدار. |
| **مخاطر** | لا يمكن ضمان «عدم النسخ» — إخلاء مسؤولية واضح في الواجهة. |

## 2.4 مؤشر «صفر شبكة» (Zero-Network Indicator)

| | |
|---|---|
| **ماذا** | أيقونة خضراء دائمة تقول «لا توجد أي طلبات شبكة — كل شيء محلي 100%». تتحول للحمراء لو حدث أي طلب. |
| **لماذا** | طمأنة بصرية للمستخدم الحساس (وثوق بدون كلام). |
| **تقنياً** | مراقبة `fetch`/`navigator` في وضع الإرسال/الاستقبال + شارة دائمة في الهيدر. |
| **الجهد** | صغير (S). |
| **مخاطر** | لا شيء — تحسين وثوقية فقط. |

---

# 🥉 المرحلة 3 — سلاسة واحترافية

> الهدف: واجهة تُرضي المستخدم المتقدم وتوسّع الجمهور.

| الميزة | الوصف التقني | الجهد |
|---|---|---|
| **وضع نص سريع** | لصق نص → QR مباشرة دون ملف (حاوية نصية صغيرة). | S |
| **سجل النقل المحلي** | آخر النقولات (اسم، حجم، وقت، حالة) في IndexedDB. | S-M |
| **ملفات ضبط مخصصة** | إنشاء/حفظ بروفايلات (سرعة/إصلاح/عدد مسارات). | M |
| **زر لغة (عربي/إنجليزي)** | نظام i18n خفيف (عربي افتراضي + إنجليزي). | M |
| **جولة تعليمية أول مرة** | خطوات مصوّرة متحركة عند أول استخدام. | M |
| **أداء: ضغط في Worker** | نقل الضغط والترميز لـ Web Worker حتى لا تتجمد الواجهة على الملفات الكبيرة. | M |
| **تدفق للملفات الكبيرة** | معالجة مقسمة (streaming) لتقليل استهلاك الذاكرة عند 512MB+. | L |
| **اختصارات لوحة المفاتيح** | للمرسل على الحاسوب (اختيار ملف، بدء/إيقاف، ملء الشاشة). | S |

---

# 🧭 ترتيب التنفيذ المقترح (Milestones)

| الإصدار | المحتوى | المعيار للاعتبار «منجز» |
|---|---|---|
| **v1.1** | متعدد الملفات + مشاركة أندرويد + استقبال ويب كام | اختبار E2E: نقل مجلد من ملفات + مشاركة من تطبيق + نقل موبايل→كمبيوتر |
| **v1.2** | حزمة الأوفلاين + بث جماعي | تشغيل الموقع كاملاً من ملف zip على شبكة مغلقة (بدون نت) |
| **v1.3** | ترابط QR + مؤشر صفر شبكة + حذف بعد القراءة | اختبار E2E: ترابط بلا كتابة + تحقق من عدم وجود أي طلب شبكة |
| **v1.4** | التوقيع الرقمي (وضع متقدم) | اختبار E2E: توقيع وتحقق + رفض ملفات مزوّرة |
| **v1.5** | السلاسة: نص سريع + سجل + بروفايلات + لغة + جولة | اختبارات واجهة شاملة |
| **v1.6** | الأداء: ضغط Worker + تدفق الملفات الكبيرة | نقل ملف 500MB دون تجميد |

---

# ⚠️ قيود واقعية (نكون صادقين مع أنفسنا)

1. **لا توجد قناة تغذية راجعة (feedback)** من المستقبِل إلى المرسل بصرياً — لا يمكن للمرسل «معرفة» أن المستقبل استلم. أي اقتراح يعتمد على ذلك (تكيف تلقائي للسرعة) يحتاج قناة أخرى (صوت/شبكة) — خارج نطاق «الانعزال».
2. **مشاركة أندرويد** تعمل فقط على أندرويد/Chromium ومع PWA مثبّت — iOS خارجها حالياً.
3. **الأمان الفيزيائي** يبقى مسؤولية المستخدم (من يرى الشاشة) — التشفير يحمي المحتوى، لا منع التصوير.
4. كل ميزة تُضاف مع اختباراتها — لا نقبل «ميزة بلا تحقق».

---

*آخر تحديث: 1 أغسطس 2026 — وثيقة تخطيط، لم يبدأ أي تنفيذ.*
