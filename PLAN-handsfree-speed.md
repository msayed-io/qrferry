<div dir="rtl" align="center">

# 🕊️ خطة «اليد الحرة + النقل السريع» — QRFerry v1.2

**الهدف: المستخدم يثبّت الموبايل مرة واحدة ثم يمشي — النقل يتم بدقة،
والملفات الكبيرة تنتقل في ثوانٍ بدل دقائق.**

</div>

---

## المشكلة (بأرقام)

النقل البصري QR محكوم بحد فيزيائي: الكاميرا تقرأ ~30–40KB/ثانية كأفضل
حالة عملية. النتيجة:

| الحجم | Turbo 60 مزدوج | الوضع السريع المحلي (مقترح) |
|---|---|---|
| 1 MB | ~30 ثانية | < 1 ثانية |
| 10 MB | ~5 دقائق | ~1-2 ثانية |
| 50 MB | ~25 دقيقة | ~5-10 ثواني |

إضافةً لذلك، الإمساك اليدوي الطويل: تعب، ارتجاف، ضياع تركيز، أخطاء.

---

## 🧩 المحور الأول — وضع اليد الحرة (Hands-Free) — بلا أي متطلبات شبكة

> **ماذا يحل**: المستخدم يحط الموبايل على حامل/طاولة/مشبك → يمشي → التطبيق يظل
> يستقبل بثبات ويخبره عند الاكتمال. **لا يتطلب أي تغيير في البنية أو الشبكة.**

### المكونات

| # | الميزة | التقنية | معيار القبول |
|---|---|---|---|
| 1 | **منع نوم الشاشة** | `navigator.wakeLock.request("screen")` أثناء الاستقبال والإرسال | الشاشة تبقى مضيئة طوال النشاط على الموبايل |
| 2 | **إرشاد «يد حرة» عند البدء** | رسالة في أول تشغيل الكاميرا: «ثبّت الموبايل على حامل أو استند عليه — سأراقب نيابة عنك» | الظهور مرة واحدة عند أول استخدام |
| 3 | **كشف الاستقرار** | مقارنة الإطارات المتتالية (اهتزاز منخفض) → حالة «مستقر» | عند الثبات تظهر شارة «وضع مستقر ✓ يمكنك تركه» وتتحسن دقة المسح |
| 4 | **إشعار الاكتمال** | `navigator.vibrate` + نغمة Web Audio + (اختياري) `Notification API` | عند اكتمال النقل: اهتزاز + صوت مسموع حتى لو الشاشة مقفولة للخلفية |
| 5 | **إكمال تلقائي للانقطاع** | الـ Resume الموجود يُفعَّل تلقائياً عند العودة | لو قفلت الشاشة/انقطع، يكمل من نفس الرمز عند إعادة فتح /scan |
| 6 | **أتمتة الإيقاف** | إيقاف الكاميرا تلقائياً عند الاكتمال (موجود) + تحرير Wake Lock | لا استهلاك بطارية بعد الانتهاء |

### تجربة المستخدم بعد التنفيذ
1. يفتح `/scan` → يضغط «تشغيل الكاميرا» → تظهر رسالة «ثبّت الموبايل وارح»
2. يضع الموبايل على الحامل قدام الشاشة → بعد ثانيتين: «وضع مستقر ✓»
3. يمشي — الشاشة لا تنام، المسح مستمر
4. عند الاكتمال: اهتزاز + صوت → يرجع يحفظ الملف
5. لو قفلت الشاشة: عند العودة يكمل تلقائياً

---

## 🚀 المحور الثاني — الاختيار الذكي للقناة + النقل المحلي السريع

> **ماذا يحل**: الملفات الكبيرة. QR اقتران واحد → نقل مباشر بين الجهازين عبر
> الشبكة المحلية بسرعة ميغابايت/ثانية. **لا إنترنت مطلوب** (واي فاي محلي أو
> hotspot من الموبايل). **لا سيرفر** (تبادل اتصال يدوي عبر QR أو خادم صغير
> مدمج في نسخة الأوفلاين).

### منطق الاختيار الذكي (تلقائي بالكامل)

```
يختار المستخدم الملف
      │
      ├── ≤ ~1.5 MB ──────────────► بث QR مباشر (لا يحتاج أي شبكة)
      │
      └── > ~1.5 MB ───► «الوضع السريع المحلي» متاح؟
                              │
              ┌───────────────┴───────────────┐
              │ نعم (شبكة محلية متاحة)         │ لا (معزول تماماً)
              ▼                               ▼
        QR اقتران واحد ←──────────       بث QR + وضع يد حرة
        (ثوانٍ)                            (يريح المستخدم)
              │
              ▼
        نقل مباشر سريع (WebRTC/LAN)
        ميغابايت/ثانية + تشفير DTLS
              │
              └── فشل الاتصال؟ ──► يرجع تلقائياً لبث QR
```

### مكونات المحور الثاني

| # | الميزة | التقنية | معيار القبول |
|---|---|---|---|
| 1 | **تصنيف تلقائي للحجم** | عتبة ~1.5MB مع اقتراح واضح للمستخدم | ملف كبير → يظهر اقتراح «الوضع السريع» بشكل بارز |
| 2 | **QR الاقتران** | يعرض offer الاتصال (SDP مضغوط) + مفتاح جلسة عشوائي | يمسحه الموبايل في <5 ثوانٍ ويعرض answer |
| 3 | **نقل P2P** | WebRTC DataChannel (مشفر DTLS تلقائياً) | ملف 10MB ينتقل في <3 ثوانٍ على LAN |
| 4 | **تقدم فوري** | شريط تقدم + سرعة + وقت متبقٍ (بايتات فعلية) | المستخدم يرى النسبة تصعد لحظياً |
| 5 | **إشعار الاكتمال** | نفس نظام المحور الأول | اهتزاز + صوت |
| 6 | **الوقوع التلقائي** | فشل الاتصال/انقطاع الشبكة → عودة لبث QR من نفس النقطة | النقل لا يفشل أبداً — يختار القناة المتاحة |
| 7 | **أمان** | القناة مشفرة (DTLS) + مفتاح جلسة QR + تكامل مع التشفير الاختياري الحالي | لا يمكن اعتراض النقل على الشبكة |
| 8 | **نسخة الأوفلاين** | خادم محلي صغير (داخل حزمة الأوفلاين) يقدّم signaling + نقل WebSocket | يعمل في شبكة معزولة فيها LAN |

### قيود صريحة (واقعية 100%)
- **المحور الأول**: يريّح المستخدم لكن سرعة QR نفسها (~30-40KB/s) لا تتغير — للملفات الكبيرة جداً (مئات MB) يبقى وقتاً طويلاً حتى مع الراحة.
- **المحور الثاني**: يتطلب شبكة محلية (واي فاي مشترك أو hotspot من الموبايل). في بيئة بلا أي شبكة نهائياً، يبقى QR + يد حرة هو المتاح.
- **WebRTC على شبكة منفصلة تماماً عن النت**: يعمل عبر LAN بشكل موثوق؛ عبر hotspot أندرويد يعمل أيضاً. على iOS يحتاج HTTPS (متوفر على Vercel).

---

## 🗓️ ترتيب التنفيذ المقترح

| الإصدار | المحتوى | معيار «منجز» |
|---|---|---|
| **v1.2a** | المحور الأول كاملاً (يد حرة) | اختبار E2E: Wake Lock يُطلب + شارة استقرار تظهر + اهتزاز عند الاكتمال |
| **v1.2b** | المحور الثاني: الاقتران + النقل المحلي + الوقوع التلقائي | اختبار E2E: ملف 10MB ينتقل <3ث عبر قناة محلية محاكاة + فشل اتصال يرجع لـ QR |
| **v1.2c** | نسخة الأوفلاين + خادم signaling مدمج | اختبار: نقل سريع داخل حزمة الأوفلاين على LAN |

---

## ✅ حالة التنفيذ

اكتمل تنفيذ المراحل: **v1.2a** (يد حرة) ✅ · **v1.2b** (نقل سريع) ✅ · **v1.2c** (تلفزيون) ✅

---

## 📌 خلاصة القرار

- **الأساس**: وضع اليد الحرة (فوري، بلا شبكة، يحل الإمساك)
- **الترقية**: اختيار ذكي للقناة — صغير QR / كبير سريع محلي / معزول يرجع QR
- **المبدأ**: المستخدم لا يمسك الموبايل أبداً بعد التثبيت، والملف لا يضيع أبداً

*وثيقة تخطيط — لم يبدأ التنفيذ بعد.*
